from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The project about to summarize the English content',
    author='iffishells',
    license='MIT',
)
